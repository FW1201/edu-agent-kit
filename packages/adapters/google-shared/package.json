{
  "name": "@edu-agent-kit/google-shared",
  "version": "0.1.0",
  "description": "Shared Google OAuth2 client, token store, scope registry, and consent flow for all Google adapters (Classroom, Workspace).",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@edu-agent-kit/mcp-shared": "workspace:*",
    "googleapis": "^144.0.0"
  }
}
