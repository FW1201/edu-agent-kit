{
  "name": "@edu-agent-kit/firebase",
  "version": "0.1.0",
  "description": "Firebase adapter — publish interactive teaching materials to Firebase Hosting (deploy via firebase-tools through npx).",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@edu-agent-kit/core": "workspace:*",
    "@edu-agent-kit/mcp-shared": "workspace:*",
    "zod": "^3.23.8"
  }
}
