{
  "name": "@edu-agent-kit/kahoot",
  "version": "0.1.0",
  "description": "Kahoot! adapter — generate official .xlsx import workbook (create) and read/analyze results via the Reports API.",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@edu-agent-kit/core": "workspace:*",
    "@edu-agent-kit/mcp-shared": "workspace:*",
    "exceljs": "^4.4.0",
    "zod": "^3.23.8"
  }
}
